spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/map_game_db
    username: postgres
    password: root1912
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: ${PORT:8080}